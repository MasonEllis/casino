# WebSocket proxy for the Grand Royale lobby server.
# Prefer running ./setup-lobby.sh once — it installs this automatically.
# Manual option: copy deploy/nginx-lobby-snippet.conf into your server { } block
# (or include snippets/casino-lobby-ws.conf; after copying the snippet file).