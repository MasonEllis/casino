Optional: drop your own Curb clip MP4s here to override YouTube playback.

Naming: match the episode slug, e.g.
  affirmative-action.mp4
  the-pants-tent.mp4
  palestinian-chicken.mp4

Optional fallback MP4:
  default.mp4

If no local file exists, the ATM plays a YouTube embed instead.
Add slug → video ID mappings in src/game/curbClips.ts for episode-specific clips.